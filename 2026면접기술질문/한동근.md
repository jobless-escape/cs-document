### 넵튠(크래프톤 자회사/솔루션개발)

- 프로젝트에 MSA를 도입한 이유는?
    - MSA가 항상 좋다고 생각하나?
    - 말대로라면 대기업만 MSA를 도입할 수 있는 거 아닌가? 팀당 40~50명 되는 기업은 어떻게 생각하나?
    - 그렇다면 MSA의 장점은 뭐라고 생각하나?
- 어떤 DB를 써봤나?
- 채팅에서 MySQL과 MongoDB를 같이 사용한 이유는?
- ZSort로 캐싱했다고 했는데, 그건 캐싱이 아니지 않나?
- TCP와 UDP의 차이는?
    - 왜 UDP가 실시간 게임에 적합한지
- HTTPS와 HTTP의 차이는?
    - HTTPS 요청이 들어왔을 때 처리 과정은?
    - 인증서는 누가 관리하나? EC2? Certbot?

### 아티스트컴퍼니(애드테크 코스닥 상장 중소)

- 가장 어려웠던 프로젝트는 뭐였는지
- s3 pre signed url에 대해 설명
    - 만기시간은 어떻게 했는지?
- 애플리케이션 단에서 중복 기사 검증하는 게 비효율이라 생각했는데 그럼 어떻게 한건지
    - unique index가 디비 저장 시점에서 처리했다고 말하는건지?
- 레디스 캐싱 시간 보니까 다른 것도 있었을 거 같은데 이런걸 생각하기 위한 방법
- 깃에서 원격 저장소에서 remote add 하는 거 아는가
- 개발할 때 ai 얼마나 쓰는지
    - AI 쓰면서 어떤 걸 느꼈나

### 루먼랩(스타트업)

- 알고 있는 정렬 알고리즘의 종류를 설명해주세요.
    - 버블 정렬, 삽입 정렬, 퀵 정렬 등 각각의 동작 방식과 시간복잡도를 설명해주세요.
- DB 인덱스(Index)의 개념을 설명해주세요.
    - 인덱스가 과도하게 많을 경우 발생하는 문제점은 무엇인가요?
    - 인덱스가 없는 상태에서 대량의 데이터 중 최소값 10개를 조회하면 DB는 어떤 방식으로 동작하나요?
- 메모리 누수(Memory Leak)란 무엇인가요?
- Java의 GC(Garbage Collection) 동작 원리를 설명해주세요.
- 데드락(Deadlock)과 라이브락(Livelock)의 차이점은 무엇이며, 데드락을 방지하는 방법은 무엇인가요?
- Java Stream API에서 Stateful 연산과 Stateless 연산의 차이를 설명해주세요.
    - `filter()`와 `sorted()` 중 어떤 것이 Stateful이고 어떤 것이 Stateless인지 이유와 함께 설명해주세요.
- SOLID 원칙을 설명해주세요.
    - 백엔드 아키텍처(MVC 등)에서 단일 책임 원칙(SRP)을 어떻게 적용하는지 설명해주세요.
- JVM(Java Virtual Machine)이 무엇인가요?
    - 멀티스레드 환경에서 JVM이 왜 중요한가요?
- 스프링 빈(Spring Bean)의 생명주기를 설명해주세요.
- `equals()`와 `hashCode()`의 관계를 설명해주세요.
    - `hashCode()`가 true이면 `equals()`는 반드시 true인가요?
    - `equals()`가 true이면 `hashCode()`는 반드시 true인가요?
- DB 트랜잭션(Transaction)이 무엇인가요?
    - 스프링에서 트랜잭션을 어떻게 관리하나요?
- ACID 원칙을 설명해주세요.
- RESTful API를 설계할 때 고려해야 할 사항은 무엇인가요?
- URI 설계 시 어떤 점을 고려해야 하나요?
- REST의 Stateless가 무엇인지 설명해주세요.
- HTTP 응답 코드(200, 201, 401, 403 등)는 언제 사용하는지 설명해주세요.
- 의존성 주입(DI)이 무엇인가요?
    - 스프링에서 DI는 어떻게 구현되나요?
- 가상환경(Virtual Machine)이 무엇인가요?
- 컨테이너(Container)가 무엇인가요?
- Docker가 무엇인가요?
- 고가용성(High Availability, HA)이 무엇인가요?
    - 고가용성을 높이는 방법에는 어떤 것들이 있나요?
- 브라우저에 `www.naver.com`을 입력하고 엔터를 누르면 서버 관점에서 어떤 과정이 일어나나요? (DNS 조회부터 서버 응답까지)

### 콘텐트리중앙(중앙그룹 계열사)

- SQL Injection을 설명하고 해결법을 말해주세요
- 메시지 큐에 대해 말해보세요
- 캐싱에 대해 말해보세요

### 커넥트웨이브(다나와)

- 써본 자바 버전에 대한 설명, java11이 있고 java21이 있는데 스프링 부트 프로젝트에서 이걸 왜 선택했는가
- 스프링 프레임워크 버전별로 차이점이나 특징 알고 있는지
- 스프링 버전별로 차이점이나 특징 알고 있는지. (3.3.1과 같은)
- 자바 JVM 구조에 대해 설명하고 GC에 대해 설명해달라
- 커넥션 풀에 대해 설명
- 추천 모델을 구현하는 서버를 왜 파이썬으로 구현했는지. 이유가 있었나?
- 엘라스틱 서치 성능 감소 부분에서, 단건으로 요청을 날리는 게 아니라 실제 J-Meter와 같은 부하테스트를 통해 실제 사용자들이 사용하는 것처럼 테스트를 해서 TPS를 계산했는지
- 팟캐스트는 스트림 형식으로 한건지
- 백엔드 개발자란 어떤 역할이라 생각하는가.
- 백엔드 개발자로서의 방향성이 어떻게 되는지.
- 엘라스틱서치 구조나 동작 원리에 대해 설명해봐라
- 역색인이 뭔가
- 사용할 수 있는 다른 언어들이 있는가
- 그 언어들의 장점과 단점을 알고 있는가
- 서버 사이드쪽 캐싱에 대해 설명해주세요
- 카카오톡에서 채팅을 어떻게 구현했을 거 같나 - grpc로 했을 것 같습니다.
    - grpc 구조가 어떻게 되는지 설명해봐라
- 데이터베이스 간 차이 설명해봐라(MySQL, MariaDB 말하는 건지 NoSQL과 RDBMS 차이인지 이해를 못함)
- 네트워크나 CS 지식에 대해 공부한 게 있는지. 있다면 하나 말해봐라 - TCP/UDP 차이 말함
- UDP가 왜 게임 구현 시 사용되는 지 말해보세요
- 스프링부트에서 비개인화 추천 모델 계산한 이유가 뭔가
- 이건 스프링 배치를 돌린건가? 아니면 그냥 스케줄러 어노테이션?

### 마인드허브(의료 스타트업)

- 엘라스틱서치 EC2 여러개에 나눠서 검색하는 게 장점인데 해봤냐
- 메모리 구조(힙, 스택, 데이터, 코드)영역에 대해 설명
- 알고있는 정렬 알고리즘에 대해 말해봐라
- 분할 정복 분할이랑 정복 각각 시간복잡도
- 분할할 때 왜 log N이라고 생각하나 (?)
- 트리의 시간복잡도
- 트리와 그래프의 차이점
- dfs, bfs에 사용되는 자료구조
- presigned url에 대해 설명
- 그냥 클라이언트가 s3에 이미지를 저장해도 되는 거 아닌가? 왜 서버를 거쳐서 프리사인드 url을 받아서 저장하는가